#!/usr/bin/env bash

set -euo pipefail

PROJECT_ROOT="/home/saspeh/dev/python/ncvoters"
PYTHON="$PROJECT_ROOT/.venv/bin/python"

if [[ ! -x "$PYTHON" ]]; then
    echo "Missing project virtualenv python: $PYTHON" >&2
    exit 1
fi

exec "$PYTHON" -m ncvoters "$@"
